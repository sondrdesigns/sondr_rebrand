`Card` — a clean white sheet with a hairline black border.

```jsx
<Card>content</Card>
<Card lift style={{ width: 405 }}>floats off the page</Card>
```

Props: `padded` (default true), `lift` (drop shadow), `style`. Sharp corners, 1px inset ink border — matches the source's Rectangle panel.
