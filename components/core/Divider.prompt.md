`Divider` — a thin ruled line for separating sections.

```jsx
<Divider />
<Divider variant="dashed" />
<Divider orientation="vertical" length="80px" />
```

Props: `orientation`, `variant` (`solid` | `dashed`), `color`, `length`. Uses the soft-ink margin color to read like a pencil rule.
