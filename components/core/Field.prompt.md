`Field` — a hairline text input / textarea with a mono label.

```jsx
<Field label="your name" placeholder="jane doe" />
<Field label="message" multiline rows={5} placeholder="tell us about your project" />
```

Props: `label`, `placeholder`, `multiline`, `rows`, `value`, `onChange`, `type`. Same border treatment as `Card`.
