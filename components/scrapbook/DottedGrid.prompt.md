`DottedGrid` — the notebook-paper background surface.

```jsx
<DottedGrid margin style={{ minHeight: '100vh', padding: 60 }}>
  ...page content...
</DottedGrid>
```

Props: `margin` (draw the left ruled line at 180px), `paper` (base color), `style`. This is the default canvas for nearly every Sondr layout.
