home:

- tape on featured works blocks certain text from being read
- hard to read certain text like the scroll for featured works
- hero section appears quite small in mobile- disproportionate

interior dept.:

- post it note is  completely blocking the header of the page
- page does not fixate to the screen size, there is side scroll allowed on mobile

studio:

- format of how the different members are layed out is weird (left centered and vertically aligned, so change this). toshio nagai should also be a co founder


blog:

- create a real blog page although we do not have any real articles or blogs to publish. so get rid of this in progress page
- there should also be an email sign up newsletter options for updates to the blog

works:

works could be better formatted instead of all vertically aligned
